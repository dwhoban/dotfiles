# Aliases shared by zsh and bash.
# Sourced from ~/.zprofile (zsh) and ~/.profile (sh/bash).
# POSIX alias syntax only; in zsh the OMZ eza plugin may override ls aliases.

alias ll='ls -lAh'
alias la='ls -lah'
alias ..='cd ..'
